# vdot.vv 指令设计文档（2.1）

> **赛道**：2026 CIE 全国 RISC-V 高水平创新及应用大赛 · 香山应用赛道（vdot 指令设计）
> **阶段**：第二阶段 2.1 设计文档
> **团队/作品**：cie2026-Z-riscv-xs
> **版本**：v1.1 ｜ **日期**：2026-09-12
> **对应 RTL**：`VDotCore.scala` / `VDotFu.scala`（2.2 提交，本文档为唯一设计依据）
>
> **修订记录**：
> - v1.1（2026-09-12，与 2.2 落地实现对齐）：① 指令编码 funct3 由 `100` 改为 `011`（避让 RVV 标准指令 `vminu.vx` 冲突）；② e16/e32 乘法器由 Karatsuba 位分解改为 Chisel 原生有符号乘法器（Karatsuba 32 位负数路径存在位宽截断缺陷，见 §7.2）；③ 归约累加由零扩展改为符号扩展（修正负元素乘积累加错误，见 §7.4）；④ 接入方式由"新建 VDotU + FuType.vdot"改为"接入 VIPU（FuType.vipu + VipuType.vdot_vv）"，共 9 处改动（见 §10）；⑤ 结果槽位统一写回 `vd[31:0]`（slotMask 固定低 32 位，见 §9）。
> - v1.0（2026-09-11）：初稿。

---

## 1. 概述

vdot.vv 是一条**自定义向量归约指令**：单条指令完成"逐元素乘法 → 全向量归约求和 → 累加到目标寄存器"三个操作，将 RVV 基线中 `vmul.vv` + `vredsum.vs` 两条指令（含一次额外向量寄存器写回）压缩为一条指令。

| 对比项 | 基线（vmul.vv + vredsum.vs） | vdot.vv |
|---|---|---|
| 指令数 | 2 | 1 |
| 中间写回 | 一次 vd 写回 | 无 |
| 语义 | 逐元素乘，再单独归约 | 乘+归约+累加融合 |

本指令支持 e8 / e16 / e32 三种精度，可配置有效元素数（vl），并携带定点溢出标志（vxsat），面向嵌入式 AI 推理等"内积密集"场景。

---

## 2. 指令编码

vdot.vv 采用 RVV 1.0 的 **OP-V** 指令格式，编码位于 `OPMVV` 归约子空间。**关键约束**：初版选用 `funct3 = 100`，但实测该编码与 RVV 标准指令 **`vminu.vx`**（`funct6=000100, funct3=100`）冲突——`vdot.vv v2, v1, v0` 会被香山译码为 `vminu.vx v2, v1, x0 = min(v1, 0) = 0`，导致输出恒 0 且 DiffTest 误判"通过"。经核对 rocket-chip 译码表，`funct6=000100 / funct3=011` 位置空闲，故最终编码采用 **`funct3 = 011`**。

### 2.1 编码格式（最终版）

```
31                26 25 24              20 19              15 14        12 11             7 6             0
┌──────────────────┬──┬──────────────────┬──────────────────┬───────────┬──────────────────┬──────────────┐
│      funct6      │vm│       vs2        │       vs1        │  funct3   │       vd         │    opcode    │
│      000100      │  │                  │                  │   011     │                  │   1010111    │
└──────────────────┴──┴──────────────────┴──────────────────┴───────────┴──────────────────┴──────────────┘
  vdot.vv vd, vs2, vs1, vm     → 编码 0x12103157
```

字段说明：

- `funct6 = 000100`：本指令专用编码，位于 `OPMVV` 归约扩展区。
- `vm`：按 RVV 规范，控制掩码寄存器 `v0` 是否参与计算。
- `vs2` / `vs1`：两个参与逐元素乘法的源向量寄存器。
- `vd`：目标寄存器，结果写回到 `vd[31:0]`，其余槽按尾策略处理。
- `funct3 = 011`：**修正值**（初版 `100` 与 `vminu.vx` 冲突，见上）。
- `opcode = 1010111`：RVV `OP-V` 指令编码。

### 2.2 编码分配（修正后）

```
OPMVV (funct3 = 011 子空间)
├── 000100 / 011 : vdot.vv   ← 采用编码（rocket-chip 译码表确认空闲）
└── 其余 : 保留 / 扩展

（注：funct3 = 100 子空间含 vminu.vx 等标准指令，初版编码 0x12104157 已废弃）
```

编码策略：避开 RVV 标准指令占用的 `funct6/funct3` 组合，选 rocket-chip 译码表中确认为空闲的位置，满足"新增指令、最小侵入、兼容性优先"。

---

## 3. 指令语义

### 3.1 伪代码

```
vdot.vv vd, vs2, vs1, vm:
  for i in 0 .. vl-1:
    if vm == 0 and v0[i] == 0: skip        # 掩码元素跳过
    acc += sign_ext(vs1[i], SEW) * sign_ext(vs2[i], SEW)   # 乘积精确（2×SEW 位）
  vd[31:0] = acc + vd_old[31:0]            # 累加旧值，截断到 32 位标量
  if 累加溢出: vxsat = 1                    # 当前策略：置位但不饱和
  vd[127:32] = oldVd[127:32]               # 高槽保留旧值（ta=0）或全 1（ta=1），由外壳处理
```

### 3.2 三种精度行为

| SEW（vsew 编码） | 元素数（VLEN=128） | 乘积宽度 | 累加目标槽 | vd 其余槽 |
|---|---|---|---|---|
| e8（`00`） | 16 | 16 位（精确） | vd[31:0] | 保留旧值（ta=0） |
| e16（`01`） | 8 | 32 位（精确） | vd[31:0] | 保留旧值（ta=0） |
| e32（`10`） | 4 | 64 位（精确） | vd[31:0] | 保留旧值（ta=0） |

> **说明（v1.1 修订）**：vdot 产生一个 **32 位标量**（点积和截断到 32 位有符号），统一写回 `vd[31:0]`（`slotMask` 固定低 32 位），高 96 位按尾策略（ta）由外壳处理。初版设计中 e16 写 `vd[63:0]`、e32 写 `vd[127:0]` 的方案已废弃——点积和按 32 位标量语义截断，与 2.3 测试用例（e16=36、e32=-128 均在 32 位内）一致。

### 3.3 边界规则

1. **vl 掩码**：仅 `i < vl` 的元素参与归约，其余元素贡献 0（由内核掩码实现，见 §7.4）。
2. **尾元素**：`i ≥ vl` 的 vd 槽按 RVV 尾策略（vta）由外壳处理；本内核输出 0。
3. **溢出**：乘积不溢出（宽度 2×SEW 精确），仅**累加阶段**可能溢出。当前策略为**截断 + vxsat 置位**（VDotCore 中 `acc(128)` 为溢出位）；如需饱和语义可在累加器扩展（§9 有说明）。
4. **vstart / 掩码**：按 RVV 规范由外壳（VecPipedFuncUnit + Mgu）处理，内核接口不含 vstart（对齐 Reduction.scala 的归约范式）。

### 3.4 与阶段三 NEMU 的契约

本文档 §3.1–§3.3 语义是阶段三 NEMU 参考实现（`vdot.vv` 模拟逻辑）的**唯一依据**。NEMU 端必须实现：三精度符号乘法、vl 掩码、与 vd[31:0] 累加、截断 + vxsat。

---

## 4. 设计目标与约束

### 4.1 对标基线

参考实现 nanhu-vdot（arXiv 2409.00661）采用**固定 8-bit 乘法器阵列**（8 路 8×8 乘法器 + 7 个加法器，输出 64 位），仅支持单一精度。本设计支持 **e8/e16/e32 三精度**。

### 4.2 设计约束

| 约束 | 取值 | 依据 |
|---|---|---|
| VLEN | 128 位 | 香山向量寄存器位宽 |
| SEW | e8 / e16 / e32 | 比赛 2.3 测试要求（正/负/边界） |
| 结果精度 | 精确（乘积 2×SEW 位），累加截断到 32 位标量 | 边界值测试需精确比对 |
| 延迟 | 2 拍（CertainLatency(2)） | 乘+归约+累加两拍完成 |
| 吞吐 | back-to-back，1 条/周期 | piped=true + 流水化 |

### 4.3 优化边界（算法选型）

> **定理（下界）**：精确计算任意两个长度 K 的运行时向量的点积，至少需要 K 次乘法。优化空间只在面积、延迟、吞吐三个维度。

经调研，Fast Inner Product（FIP，Winograd 1968）及 Free-Pipelined FIP（arXiv 2311.12224）等"减乘法"算法依赖多输出间的中间结果复用（α/β 项），仅适用于矩阵乘法/脉动阵列场景；位层 MAC、分布式算术、查表法分别依赖权重稀疏性、固定系数等前提，均与 vdot.vv 的单指令、运行时操作数、精确计算约束不兼容。单条点积的乘法次数下界为 K，本设计将优化集中于微架构层：**Radix-4 Booth 编码乘法器**减半部分积（e8）；**Chisel 原生有符号乘法器**保证 e16/e32 正确性与时序收敛；**流水化**实现 back-to-back 发射。e16/e32 曾尝试 **Karatsuba 位分解**复用 8×8 基元以压缩面积，但验证发现其 32 位负数路径存在位宽截断缺陷，最终**弃用**、改用原生乘法器（详见 §7.2 修正记录）；面积复用方案保留为工程化方向（§12）。

---

## 5. 微架构总览

```
                            ┌──────────────────────────┐
     vs1[128] ─────────────▶│       数据切分/解码      │
     vs2[128] ─────────────▶│   (sew, vl, vm, vstart) │
     vdOld[128] ───────────▶│                            │
     sew / vl / vm ───────▶│                          │
                            └────────────┬─────────────┘
                                         │
                                         ▼
                            ┌──────────────────────────┐
                            │   乘法器组（VDotCore）    │
                            │  e8:  Booth 8×8      ×16 │
                            │ e16: Chisel 原生 16×16 ×8│
                            │ e32: Chisel 原生 32×32 ×4│
                            └────────────┬─────────────┘
                                         │
                                         ▼
                            ┌──────────────────────────┐
                            │  归约累加（符号扩展）      │
                            │ Σ prod(i)·sign_ext，128位 │
                            │  + vdOld[31:0] + vxsat   │
                            └────────────┬─────────────┘
                                         │
                                         ▼
                            ┌───────────────────────────┐
                            │       结果打包输出         │
                            │  vd[31:0] = 累加结果       │
                            │  高槽按 tail / mask 处理   │
                            └────────────┬─────────────┘
                                         │
                                         ▼
                                  vd[128] / vxsat
```

### 5.1 两阶段执行模型

- **阶段 1（第 1 拍）**：按 `sew` 进行元素切分；对每个元素执行有符号乘法（e8 走 Booth、e16/e32 走 Chisel 原生乘法器）。
- **阶段 2（第 2 拍）**：符号扩展归约求和；与 `vdOld[31:0]` 累加；检测 `vxsat`；生成 `vd[31:0]` 结果。

### 5.2 流水特性

两拍之间插入寄存器，形成典型的 `piped` 流水结构；在 `piped = true` 时，第 `N+1` 条指令可在第 2 拍完成后立即进入第 1 拍，延迟被流水层叠掩盖，维持 back-to-back 发射能力。

### 5.3 设计意图

该微架构按"先并行乘法、再符号扩展归约、最后写回结果"的思路组织：

1. 先保证三种精度（e8/e16/e32）都能被统一数据通路处理；
2. 再在归约阶段以符号扩展保证负元素乘积累加正确；
3. 最终以最小控制开销完成 `mode / vl / mask / vxsat` 等约束处理。

---

## 6. 数据切分（SEW 解码）

按 `sew` 将 128 位源数据切分为元素组（VDotCore.scala 数据切分段）：

| sew | 元素位宽 | 元素数 | 切分逻辑 |
|---|---|---|---|
| `00` | 8 | 16 | `vs1(8i+7, 8i)`，i=0..15 |
| `01` | 16 | 8 | `vs1(16i+15, 16i)`，i=0..7 |
| `10` | 32 | 4 | `vs1(32i+31, 32i)`，i=0..3 |

切分后的元素按**有符号**送入乘法器组（`asSInt`）。三种精度的乘法器组**全部实例化**，由 `MuxLookup(sew)` 选择归约结果（`dot128`），正确性优先；工程化可改为按 sew 共享乘法器组（§12 面积讨论）。

---

## 7. 乘法器组与归约（微架构核心）

### 7.1 Radix-4 Booth 乘法器（8×8，e8 用）

实现：`BoothMultiplier(n)`（VDotCore.scala），n=8 为 e8 基元。

**原理**：每拍扫描乘数 3 个连续位（含上一组最高位形成的重叠窗口），对乘数执行 +0/+1/+2/−1/−2 五种操作，部分积数量**减半**（8 位乘法：8 个部分积 → 4 组）。

**实现要点**：
1. **符号扩展**：被乘数统一扩展至 `2n+2` 位中间位宽（`w = 2n+2`），`2a` 为 `w+1` 位，保证负项取反不错位；
2. **负项修正**：`−a` / `−2a` 先按位取反，随后 `+1` 修正位**单独累加**（`raw +& Mux(isNeg, 1, 0)`），完成补码取负；
3. **窗口合法**：`bExt = Cat(sign, b, 0)` 扩展为 `n+2` 位，最大窗口上界 `2·ceil(n/2) ≤ n+1`，索引不越界；
4. **移位求和**：第 i 组部分积左移 `2i` 位后逐级相加，截取低 `2n` 位。

**收益**：部分积 8→4，加法树规模减半，关键路径缩短。实测 e8 全部用例正确（正 136 / 负 -136 / 边界 2032 / vl=4→10）。

### 7.2 乘法器选型修正：Karatsuba → Chisel 原生乘法器（e16/e32）

**初版方案**：e16/e32 采用 **Karatsuba 位分解**，递归到 8 位 Booth 基元，复用 8×8 乘法器组以压缩面积：

```
(a·2^h + b)(c·2^h + d) = ac·2^2h + [(a+b)(c+d) − ac − bd]·2^h + bd
                            ↑ 3 个子乘法（替代 4 个）
```

**验证发现（v1.1 修正）**：`Karatsuba.signed` 在 **32 位负数路径**存在位宽截断缺陷——实测 `-3 × 2` 输出 `0xffffff583837fff8`（应为 `-6 = 0xfffffffffffffffa`），e32 混合用例 4 个乘积全部错误（-1184308404 ≠ -128）。用 Python 按 Chisel 语义（补码左移 + 固定位宽截断）精确复现了该缺陷，定位在递归乘法的符号位扩展与截断逻辑。

**最终方案**：e16/e32 改用 **Chisel 原生有符号乘法器**（`io.vs1(...).asSInt * io.vs2(...).asSInt`）：

- 逻辑等价（有符号乘法，乘积 2×SEW 位精确）；
- 正确性由 Chisel/综合工具保证，规避自研乘法器的位宽风险；
- 面积代价可接受：e16 仅 8 个 16×16、e32 仅 4 个 32×32 乘法器；
- 实测 e16（36）、e32 正数（10）、e32 混合（-128）全部正确。

> **工程化方向**：如需极致面积，可在修复 Karatsuba 符号扩展缺陷后恢复位分解方案，或按 sew 时分复用同一组乘法器（§12）。当前版本以**功能正确性优先**。

### 7.3 乘法器组账目（最终版）

| 精度 | 乘法器 | 数量 | 乘积宽度 |
|---|---|---|---|
| e8 | `BoothMultiplier(8)` | 16 | 16 位 |
| e16 | Chisel 原生 `*`（16×16） | 8 | 32 位 |
| e32 | Chisel 原生 `*`（32×32） | 4 | 64 位 |
| **合计** | | **28 个乘法器** | |

（Karatsuba/KaratsubaMultiplier/CSATree 类保留在 VDotCore.scala 中作为结构参考，未接入主通路。）

### 7.4 vl 掩码与归约（符号扩展）

实现：VDotCore.scala 归约段。

```
sumE8  = Σ_{i<16} Mux(i < vl, Cat(Fill(112, prod8(i)[15]),  prod8(i)),  0(128))   # 符号扩展
sumE16 = Σ_{i<8}  Mux(i < vl, Cat(Fill(96,  prod16(i)[31]), prod16(i)), 0(128))
sumE32 = Σ_{i<4}  Mux(i < vl, Cat(Fill(64,  prod32(i)[63]), prod32(i)), 0(128))
dot128 = MuxLookup(sew)(sumE8, sumE16, sumE32)
```

> **v1.1 修正（关键）**：初版用**零扩展**（`Cat(0(...), prod.asUInt)`）累加，并错误地认为"零扩展保留了补码语义"。实测证明该论断错误：负数乘积（如 `-1 = 0xFFFF`）零扩展到 128 位变成正数 `65535`，导致 e8 负数用例输出 `1048440`（= 65520..65535 的等差和，与理论值完全吻合）而非 `-136`。
>
> **修正**：改为**符号扩展**（`Fill` 复制乘积符号位后再拼接），负乘积在 128 位累加器中以补码正确参与加法。修正后 e8 负数 = -136 ✅。

---

## 8. 归约加法结构

### 8.1 归约实现

三种精度的元素乘积经符号扩展后，由 Chisel `reduce(_ +& _)` **链式加法**完成归约（128 位累加器），加法树结构由综合工具自动优化。代码库中保留 `CSA`（进位保存加法器，3:2 压缩）与 `CSATree`（Wallace 风格递归压缩树）类实现，可作为手工优化加法树深度的备选，但**当前主通路未使用**（正确性优先，链式加法足够满足 2 拍时序）。

### 8.2 备选：CSA/Wallace 结构（参考）

- **CSA**：`sum = a ^ b ^ c`，`carry = Cat(majority(a,b,c), 0)`（**carry 已左移 1 位，权重 2**，直接相加即正确）；
- **Wallace 树**：递归 3→2 压缩，末级进位传播加法器，加法深度约 3 级。

如后续时序不收敛，可将链式加法替换为 CSATree（工程量小，接口已就绪）。

---

## 9. 累加与溢出

实现：VDotCore.scala 累加段 + VDotFu.scala（slotMask / tail）。

```
oldSlot  = vdOld[31:0]                                    # 累加旧值（统一取低 32 位）
acc      = oldSlot +& dot128                              # 128 位累加
vxsat    = acc(128)                                       # 溢出位
result   = acc[31:0]                                      # 截断到 32 位标量
```

- **累加语义**：与目标寄存器旧值累加（`vd = dot + vd_old`），对应 RVV 归约的"初始值"概念；
- **溢出策略**：截断 + vxsat 置位。如需饱和语义，将末级加法改为"检测 acc(128) 后输出 0x7FFF…F / 0x8000…0"即可（工程量约 10 行，留作扩展点）；
- **slotMask（VDotFu）**：固定 `0x00000000ffffffff`——vdot 结果统一写 `vd[31:0]`，高 96 位由尾策略处理（`tailVd = (oldVd & ~slotMask) | (redVd & slotMask)`，ta=0 保留旧值；ta=1 全 1）；
- **已知限制**：点积和截断到 32 位标量。若 e16/e32 的点积和超出 32 位有符号范围（|sum| ≥ 2^31），高位被截断、仅 vxsat 置位。2.3 测试用例（e16=36、e32=-128）均在 32 位内，符合预期。

---

## 10. 数据通路与香山集成（v1.1 重写）

### 10.1 接入方式

**v1.1 修订**：初版设计为"新建 `VDotU.scala`（仿 VIMacU）+ 新增 `FuType.vdot` 类型"。实际落地采用**更小侵入**的方案：复用香山现有向量流水功能单元 **VIPU**（`FuType.vipu`），新增 `VipuType.vdot_vv` 操作类型，计算内核挂在 VIPU 的 VIAlu 下——与 `vredsum.vs`（Reduction）同构，共享译码、发射、写回通路。

```
backend/fu/wrapper/VIPU.scala（VIAluDecoder 增 vdot_vv 映射）
        │ 调用
        ▼
yunsuan/vector/VectorALU/VDotFu.scala（功能单元外壳：输入锁存、slotMask、tail）
        │ 调用
        ▼
yunsuan/vector/vdot/VDotCore.scala（计算内核：乘法 + 归约 + 累加）
```

### 10.2 九处接入点清单（实际落地）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `backend/decode/Instructions.scala` | 新增 `VDOT_VV` BitPat（编码 `0x12103157`） |
| 2 | `backend/decode/VecDecoder.scala`（L413 附近） | `VDOT_VV -> OPMVV(FuType.vipu, VipuType.vdot_vv, ..., UopSplitType.VEC_VRED)`，与 `VREDSUM_VS` 同构 |
| 3 | `backend/fu/wrapper/VIPU.scala` | `VIAluDecoder` 添加 `vdot_vv -> List(vdot, sSew, sSew, sSew)` |
| 4 | `yunsuan/package.scala` | `VipuType.vdot_vv = "b01000011"`（9 位 OpType） |
| 5 | `yunsuan/vector/VectorALU/VAluDecode.scala` | `VAluOpcode.vdot = 56`（6 位 opcode） |
| 6 | `yunsuan/vector/VectorALU/VAluBundles.scala` | VDot 相关 Bundle 定义 |
| 7 | `yunsuan/vector/VectorALU/VIAlu.scala` | `Mux(opcodeS2.isVdot, vDotAlu.io.out.vd, ...)` 结果通路选择 |
| 8 | `yunsuan/vector/VectorALU/VDotFu.scala` | VDot 功能单元外壳（9 处 RegEnable S1 输入锁存，对齐 Reduction 范式） |
| 9 | `yunsuan/vector/vdot/VDotCore.scala` | 计算内核（§5–§9） |

### 10.3 功能单元配置（VdotCfg 要点）

```scala
FuConfig(
  name = "vdot",
  fuType = FuType.vipu,            // 复用向量流水功能单元
  piped = true,
  writeVecRf = true,
  writeVxsat = true,               // 截断策略：置位但结果截断
  latency = CertainLatency(2),
  destDataBits = 128,
)
```

### 10.4 外壳要点（VDotFu）

- **输入锁存**：9 处 RegEnable 在 S1 锁存 `vs1/vs2/vdOld/vl/sew/vm/ta/vstart` 等，对齐 Reduction.scala 的 `fire_reg_s1 = RegNext(fire)` 范式；
- **执行门控**：`exec = fire && vstart < vl`（vstart ≥ vl 不执行）；
- **结果写回**：`tailOut = Mux(vl===0, oldVd, Mux(ta, tailOne, tailVd))`，slotMask 固定低 32 位；
- **vxsat 输出**：`io.out.vxsat := dotSat && exec`。

---

## 11. 时序与流水线

### 11.1 两拍划分

| 拍 | 完成内容 |
|---|---|
| 第 1 拍 | 数据切分 → 乘法器组（Booth / Chisel 原生）→ 乘积符号扩展 |
| 第 2 拍 | 128 位归约累加 → 与 vdOld[31:0] 累加 → vxsat → 结果打包 |

### 11.2 back-to-back 发射

- `piped = true` + `latency = CertainLatency(2)`：发射队列允许背靠背发射；
- 外壳寄存器级（VDotFu 的 S1 锁存）承担流水切分：第 N 条指令在第 2 拍累加时，第 N+1 条已进入第 1 拍；
- **结果**：延迟 2 拍被流水隐藏，吞吐 1 条/周期。

---

## 12. 面积与性能估算

| 维度 | 估算 | 说明 |
|---|---|---|
| 乘法器 | 28 个模块（16 Booth8 + 8 原生16×16 + 4 原生32×32） | 全实例化、正确性优先；Karatsuba 复用方案修复后可降至约 1/4 |
| 归约 | Chisel 链式加法（128 位累加） | 综合工具自动优化；CSATree 备选可压至 ~3 级 |
| 关键路径 | 乘法器 → 归约累加 → 打包 | 2 拍可收敛 |
| 对比 nanhu-vdot | 其 LUT +2.8% / FF +0.9% / BRAM 0（FPGA 数据） | 本设计代价为三精度全实例化面积；工程化方向：Karatsuba 修复后复用 / 时分共享 |

---

## 13. 验证计划与结果

### 13.1 集成测试（2.3，7 用例）

| # | 用例 | 输入 | 期望 | 实测 | 状态 |
|---|---|---|---|---|---|
| 1 | e8 正数 | 1..16 · 全 1，vl=16 | 136 | 136 | ✅ |
| 2 | e8 负数 | −1..−16 · 全 1，vl=16 | −136 | −136 | ✅ |
| 3 | e8 边界 | 127×16 · 全 1，vl=16 | 2032 | 2032 | ✅ |
| 4 | vl 裁剪 | 1..16 · 全 1，vl=4 | 10 | 10 | ✅ |
| 5 | e16 | 1..8 · 全 1，vl=8 | 36 | 36 | ✅ |
| 6 | e32 正数 | 1..4 · 全 1，vl=4 | 10 | 10 | ✅ |
| 7 | e32 混合符号 | (−3,4,−5,6)·(2,−7,8,−9)，vl=4 | −128 | −128 | ✅ |

**结果：7/7 全部通过**（`./build/emu -i vdot-test-riscv64-xs.bin --no-diff`，最终日志见 `logs/stage2/vdot-test-final.log`）。

> **说明**：初版验证计划含 vdOld 累加用例（vdOld=1000 → 1136），2.3 提交要求为正/负/边界三类，实际 7 用例已覆盖；vdOld 累加语义由 §9 定义，可在单元测试中补充。

### 13.2 验证方法

- **仿真环境**：香山 `./build/emu`（Verilator，含 vdot 自定义指令），`--no-diff` 模式（vdot 为自定义指令，NEMU 参考模型不识别，差分无验证意义）；
- **波形**：`--dump-wave` 生成 VCD/VPD，GTKWave 中追踪 vdot.vv 从取指→译码→发射→执行→写回全路径；
- **定位手段**：`[VIPU]/[VIALU]/[VDOT]/[VCORE]` 调试 printf（已随验证完成删除）。

### 13.3 修复记录（v1.1 汇总）

| 阶段 | 现象 | 根因 | 修复 |
|---|---|---|---|
| 编码冲突 | 输出恒 0，差分"通过" | 0x12104157 与 `vminu.vx` 冲突 | funct3 100→011，0x12103157 |
| 仿真超时 | 60s 报死锁 | vdot 大组合逻辑拖慢仿真约 5 倍 | 超时 ≥300s |
| slotMask | 2032 被截成 240 | 按 SEW 截断槽位 | 固定低 32 位 |
| e8/e16 负数 | 1048440 ≠ -136 | 归约零扩展累加 | 符号扩展（§7.4） |
| e32 混合 | -1184308404 ≠ -128 | Karatsuba 32 位负数缺陷 | 原生乘法器（§7.2） |

---

## 14. 参考文献

1. RISC-V International. *RISC-V "V" Vector Extension, Version 1.0*. https://docs.riscv.org/reference/isa/v20260120/unpriv/v-st-ext.html
2. Winograd, S. *A New Algorithm for Inner Product*. IEEE Trans. Computers, C-17, 1968: 693–694.
3. arXiv 2409.00661: *nanhu-vdot*（固定 8-bit VDOTU，FPGA 面积数据与本设计对比基线）.
4. arXiv 2311.12224: *Free-Pipelined FIP*（评估后不适用，见 §4.3）.
5. 香山源码：`backend/fu/wrapper/VIPU.scala`、`backend/decode/VecDecoder.scala`、`yunsuan/vector/VectorALU/Reduction.scala`（接入模板与归约范式）.
6. 本作品 2.2/2.3 RTL 与测试：`VDotCore.scala` / `VDotFu.scala` / `vdot-test.c` / `2.3-功能验证报告.md`.
